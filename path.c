#include "./include/minshell.h"

/* void obtener_dir(){

    // Obtener el directorio de inicio (HOME)
    char* homeDirectory = get_home_directory();
    if (homeDirectory != NULL) {
        printf("Directorio de inicio: %s\n", homeDirectory);
        free(homeDirectory);
    } else {
        printf("La variable HOME no está definida.\n");
    }
} */

